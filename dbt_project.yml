# dbt_project.yml
name: my_new_project
version: "1.0.0"
config-version: 2
profile: default
 
models:
  my_new_project:
    +schema: testing     # default schema under the connected DB
    prestage_1:
      +schema: consumption
      +materialized: table